{
  "description": "Auto-cleanup tmux slot state when Claude Code sessions end",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "test -n \"$SESSION_ID\" && bash \"${CLAUDE_PLUGIN_ROOT}/scripts/update-slot-state.sh\" --cleanup-session \"$SESSION_ID\" || true",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
