Review code changes for issue #ISSUE. ISSUE REQUIREMENTS: [paste issue body] FOCUS AREAS: (1) REQUIREMENT COMPLIANCE: Does the implementation satisfy every explicit requirement in the issue body? Check for must/should/has-to statements, specific templates or artifacts mentioned, ordering constraints, and hard contracts. If the issue says 'use standard depo template' but the code uses a different template -- that is P0. (2) DOMAIN CONSTRAINTS: Convex internalMutation cannot be called from Next.js API routes. Cloudflare Workers are stateless. Convex HTTP API returns 200 for thrown mutations (check body not status). Modal .env() for deploy-time, modal.Secret for runtime. Speaker correction produces names, formatting produces role labels (separate stages). LCS not sequential for word timing. (3) THE 2AM INCIDENT TEST: Would this code cause an incident at 2AM? Check concurrent access patterns, state management edge cases, error handling completeness, integration boundary failures. (4) For E2E test changes: does the test use the correct template/artifact specified in the issue? (4b) STRUCTURAL-CHANGES-IN-TEMPLATE GATE (HARD RULE per Rajiv 2026-04-30 18:48): If the diff modifies `modal/audio/processor.py` or `modal/docx/processor.py` AND adds/restores/preserves/patches a structural attribute (page-break, section break, heading geometry, indent, line spacing, paragraph structure, page margins, rulers), flag as P0: 'Structural change in pipeline — wrong place. Move to template-level (TipTap node in stored content, template style schema, source DOCX, or migration mutation). Editor↔export parity rule violated.' Anti-pattern: PR #3924 #3920 b59ccc6da. See `.claude/rules/20-buddhi-dev.md` STRUCTURAL CHANGES BELONG IN TEMPLATES + `.claude/rules/21-lessons.md` HIGH lesson. (5) E2E ASSERTION GATE (HARD RULE per Rajiv 2026-04-29): If the change modifies any value the auto-process pipeline emits into the editor or exported DOCX (template repair `convex/templates.ts`, modal pipeline `modal/`, editor `lib/editor/`, autoprocess orchestration `convex/autoprocess.ts`, DOCX export `lib/docx/`, formatting rules, paragraph attrs, OOXML elements, page-breaks, DSL rules, transforms), the PR MUST include an E2E assertion that catches the regression. Path coverage ≠ assertion coverage — exercising the buggy path without asserting on the buggy output silently passes regressions (`feedback_e2e_coverage_path_vs_assertion_distinction.md`). Domain map: `convex/templates.ts` repair → `tests/e2e/specs/auto-process-regression.spec.ts`; `modal/audio|docx/processor.py` → `auto-process-regression.spec.ts`; `lib/editor/` → `smoke-large-file-perf.spec.ts`; DOCX export → `errata-sectpr-regression.spec.ts` or `auto-process-regression.spec.ts` export-step; `convex/autoprocess.ts` → `auto-process-regression.spec.ts`. Read the candidate assertion block — do not assume "the spec already covers it." If the spec exercises the path but lacks the specific assertion (count of `<w:p>`, presence of `<w:br w:type="page"/>` at position N, attr value), flag as P0: 'E2E assertion missing for <field> in <spec>:test(N) — path is exercised but assertion is absent. Add expect(...) for <specific output>.' Unit-test-only coverage on output-affecting changes is P0 reject (PR #3924 incident). For OOXML scoping use tempered greedy `(?:(?!</closeTag>)[\s\S])*?`, not lazy `[\s\S]*?`. (6) TYPE-SHAPE CONSUMER AUDIT: When the diff adds or modifies fields on a typed object that flows through React props or function arguments, run a grep for the parent type and every helper that constructs a prop subset (look for object-literal slices like `{ field1, field2, ... } satisfies SomeType` or destructuring `const { a, b } = template`). For each consumer, classify as (a) passes-through (low risk), (b) re-shapes/slices into a new prop literal (HIGH RISK — must explicitly thread the new field), or (c) destructures specific fields (HIGH RISK). Flag as P0 any HIGH-RISK site that is not modified in the diff. Concrete pattern (PR #3681 R1 bug): the create-template wizard `app/templates/new/page.tsx` slices the template object into a new sidebar prop, so any new `TemplateLineSettings` field must be explicitly added to that slice — a grep on the type alone misses this site because it is a re-shaping boundary, not a direct caller. (7) UI DUPLICATION CHECK: When the diff adds a new UI control (form input, label, select, toggle, slider, button-with-state) to a settings panel, sidebar, dialog, or form, enumerate EVERY other field rendered in the same parent container. If two fields target the same user-facing concept (same DOCX export property, same CSS attribute, same Convex/data field, same template setting), flag as P1: two visible controls for one property is a UX defect — the user does not know which one wins. Concrete pattern (PR #3681 R2 bug, found by Rajiv during manual testing): added a 'Cover Page' override entry to a SECTION LINE SPACING list in `components/templates/template-editor-toolbar.tsx`, while the same panel already exposed a top-level 'Cover Line Spacing (pt)' field controlling the same DOCX cover-page line-spacing property. Two controls visible simultaneously, no disambiguation. Code review missed it because the diff added new lines without inspecting adjacent rendered code. Fix: when adding any new field to a settings panel `*.tsx`, render a mental snapshot of the panel and grep the same component file for sibling controls with overlapping concept names (e.g., new "Cover Page" → grep for "Cover" in the same file). (8) NO LONG-FILE PROOFREAD/FORMAT IN E2E: When the diff adds or modifies any test under tests/e2e/specs/ that triggers proofread (Cmd+Shift+P, /proofread endpoint, proofreadFullTranscript) or format (Cmd+Shift+F, /format endpoint, AI format) on a fixture larger than ~10k chars (SEED_PROJECT_LONG, SEED_PROJECT_PROOFREAD, any 100k+ char transcript), flag as P0 — REMOVE the test. CI E2E is fixture-replay only; proofread/format on long files goes through real Gemini and blows the e2e timeout (5-10min per attempt × 3 retries = 30+ min on a single test, blocking entire cascade). Long-file proofread/format perf is verified by manual QA + production telemetry, not CI E2E. Concrete pattern (PR #3854 incident 2026-04-29): smoke-large-file-perf.spec.ts:733 'Proofread large transcript perf (#3671)' triggered real proofread on Hazlehurst 5h depo every CI run, blew 20m→25m→35m→45m timeout budgets, blocked entire cascade-merge for hours. Test was removed; rule prevents recurrence. Acceptable variants: gate the test to a short fixture (~1k chars), skip the test outright, or convert to a unit test that asserts the longtask boundary on synthetic input. (9) NO USE_MODAL_STUB REFERENCES: USE_MODAL_STUB env var was removed by #3339 + #3374 (Apr 9 2026). Any new code, comment, or test referencing USE_MODAL_STUB is dead. Flag as P1 — replace with LLM proxy fixture path or remove. (10) UI-STORAGE PARITY FOR CONFIG: When the diff modifies `default_rules.yaml`, `formattingRulesYaml`, `dsl_vars`, template variables, or any user-configurable field that the pipeline reads at runtime AND the editor/template UI displays in a settings/Variables panel, verify BOTH surfaces reflect the change identically after merge. If the pipeline applies a runtime semantic transform (integer→tabs, space-count→tab-count, pt→twips, raw int→derived display) that the UI does NOT also apply, flag as P0: 'Pipeline applies <transform> at runtime but UI shows raw stored value — QC will see one thing, exported DOCX will produce another.' Concrete check: grep the field name in `app/(dashboard)/dashboard/templates/editor/`, `components/templates/`, and any `Variables` panel renderer; confirm those callers either (a) apply the same transform, or (b) read the new storage shape directly. Reference pattern (PR #3856 → #3918 follow-up): #3856 added Modal-side `int→tabs` conversion for `colloquy_tab=3` → `\t\t\t`. The diff was Modal-only; UI Variables panel still rendered the legacy space-count "15 (= 3 × 5)". QC-facing divergence broke template-verification workflow. Code review should have run a grep on the field name in editor + UI surfaces and flagged the missing UI rendering update before merge. (11) SCHEMA CHANGE → SEED REFRESH PAIRED-FILE GUARD (HARD RULE per Rajiv 2026-05-05 11:51 IST): If the diff modifies `convex/schema.ts`, `convex/migrations.ts`, or any file matching `convex/migrations/*.ts` or `convex/migrations*.ts`, the same diff MUST also modify `tests/e2e/fixtures/preview-seed.zip`. Flag as P0: 'Schema/migration change without preview-seed.zip refresh — Phase 8b not run. Per-PR Convex preview infra (#3986) seeds from preview-seed.zip; without refresh, downstream PR E2Es fail against stale snapshot. Run `env -u CONVEX_DEPLOY_KEY CONVEX_DEPLOY_KEY="$(grep ^CONVEX_DEV_DEPLOY_KEY= .env.local | cut -d= -f2-)" npx convex export --path tests/e2e/fixtures/preview-seed.zip --include-file-storage` then commit + push. See tests/e2e/fixtures/README-preview-seed.md.' Override hatch SCOPE (Rajiv directive 2026-05-09 21:40 IST — TIGHTENED): PR body line "Seed refresh skipped: <reason>" is VALID ONLY for: (a) index-only changes to EXISTING tables (e.g., adding `.index("by_foo")` to an existing `defineTable`), (b) comment/JSDoc-only changes to schema (no semantic change), or (c) field-rename in existing table with no semantic change (no new field, no removed field). Override hatch is INVALID (and Convex import WILL fail with table-ID collision) for: (i) any new `defineTable(...)` block (Convex allocates table-IDs at import time; new table absent from snapshot collides with existing seeded table-ID space), (ii) any new field on existing table (Convex schema validator may reject seed rows missing the new field), or (iii) any schema-removal (table or field). If the PR adds a new table and the override hatch is cited, REJECT the PR (P0 blocker, NOT P2 informational) and require: (1) `bun run scripts/regenerate-preview-seed.sh` (or canonical export command via env-unset wrapper to avoid CONVEX_DEPLOY_KEY hang), (2) push refreshed `tests/e2e/fixtures/preview-seed.zip` in the same PR, (3) re-run CI. Validated incident: 2026-05-09 21:35 IST PR #4265 #4259 Phase A — slot author cited "additive-only override" for new `stageRuns` table; Convex import failed with `New table 'templateRulesHistory' has IDs that conflict with existing table 'stageRuns'`. Seed refresh + re-push fixed it. See `feedback_seed_refresh_phase_8b.md` for full Override hatch SCOPE subsection. For genuinely-valid overrides (a/b/c above) flag as P2 informational rather than P0 blocker when the override note is present and plausible. (12) E2E CORE-SPEC SKIP DETECTION (HARD RULE per Rajiv directive 2026-05-05 12:59 IST): If the PR diff adds a `.skip(`, `test.skip(`, `describe.skip(`, `it.skip(`, `test.skip.if(`, or any conditional skip call inside any file under `tests/e2e/specs/core/` (post-#4063 folder layout), or — pre-#4063 — inside any of the named core specs (`tests/e2e/specs/auto-process-regression.spec.ts`, `tests/e2e/specs/smoke-project-upload.spec.ts`, `tests/e2e/specs/template-vars-fidelity.spec.ts`, `tests/e2e/specs/core/smoke-large-file-perf.spec.ts`, `tests/e2e/specs/auto-process-template-export.spec.ts`), flag as P0 REJECT. Finding type: process-violation. Required action by slot: revert the skip; investigate the underlying test failure; either (a) fix the app bug, (b) fix the test if the test itself is genuinely wrong (prove it, do not claim "flaky"), or (c) `/escalate` to PM with a real diagnosis. Skipping a core E2E spec is forbidden — these specs cover product-critical happy paths that may never silently regress. Also flag as P0 any diff that REMOVES a `test(...)` definition from a core-spec file (catches "delete instead of skip" bypass). (13) ERROR/WARNING COPY PRODUCT-MEMORY CHECK: When the diff modifies `app/api/format/route.ts`, `app/api/proofread/route.ts`, `app/actions/formatting.ts`, or any error/warning string returned to the client on the proofread or format paths, search the diff (added AND preserved/unchanged adjacent lines) for partial-selection advice — `"select a smaller section"`, `"select smaller text"`, `"try a smaller selection"`, `"reduce the selection"`, `"shorter selection"`, or any phrasing that tells the user to make their selection smaller. PRODUCT MEMORY: HeyDonna's proofread + format paths are ALWAYS full-section operations — the user does not select a range; the operation always runs on the entire current section. Only the edit-selection-with-AI feature is partial-selection. "Select smaller" copy is misleading on full-section paths because the user CAN'T reduce the section size — they need a different model. If the diff introduces such copy, flag as P1: 'Misleading error copy on full-section path. Replace with model-pivot guidance: "AI model couldn't process this section. Try a different model in your settings." or similar.' If the diff KEEPS existing misleading copy on a touched line, also flag as P1 with the same guidance — preserving misleading copy on a touched line is equivalent to introducing it. Recommended replacement copy: "AI model couldn't process this section. Try a different model in your settings." or "Section too large for current model. Switch to a higher-capacity model in settings." Reference: Rajiv directive 2026-05-05 19:43 IST DM — `app/api/format/route.ts:691, :821` flagged for "Please select a smaller section of text" on a full-section path. (14) LLM PROXY IMPACT — async webhook handling (HARD RULE per Rajiv directive 2026-05-05 20:54 IST): When the diff introduces or modifies new outbound HTTP from `modal/` (Modal → Scribie/Gemini/WhisperX/LLM/any external service), inspect the call shape. If the diff replaces a sync polling pattern with an async callback URL, OR introduces a new async webhook flow (Modal POSTs to external service, external service later POSTs back to a callback route like `/api/<x>-callback`), verify a test-side replay mechanism exists in the same diff or referenced helper: (a) a helper or fixture that synthesizes the callback body and POSTs it to the callback route when `LLM_PROXY_ENABLED=true`, OR (b) a Modal-side proxy-mode short-circuit (e.g., `if os.getenv('LLM_PROXY_ENABLED'): emit_callback_inline(...)`). Reject as P0 when the E2E test in proxy mode has no path to deterministically receive the callback — the test will hang at the workflow timeout. The proxy (`modal/shared/llm_proxy_server.py`) intercepts the outbound POST but the external service never runs in proxy mode, so it never POSTs the callback back. Path coverage ≠ replay coverage — exercising the new endpoint without a callback-replay path silently regresses to a 25m hang. Reference: PR #3953 #3945 — diff lacked test-side replay → smoke-cfr-audio-highlights test 1 hung at 25m budget on every CI run. Concrete check: grep the diff for `requests.post`, `httpx.post`, `urllib.request`, `fetch(` inside `modal/`; for each new outbound call, verify either a sync return-value path or a proxy-mode short-circuit exists; if neither, P0 reject with: 'Async webhook flow without proxy replay path. Add test-side callback synthesizer (preferred) or Modal-side `LLM_PROXY_ENABLED` short-circuit before merge.' (15) AFFECTED TESTS GATE (HARD RULE per Rajiv directive 2026-05-05 21:53 IST): When the diff modifies ANY of (a) user-facing strings (toast title/description, error messages, button labels, alert text, modal copy, dialog content, validation messages, status badges, notification text); (b) function/method signature (added/removed/renamed args, return-type change); (c) public API endpoint shape (request/response field names); (d) schema field names or types; (e) public class/interface names exported from `lib/`, `app/api/`, `convex/`, `modal/`, the reviewer MUST grep ALL test files (`tests/`, `__tests__/`, `*.test.ts`, `*.test.tsx`, `*.spec.ts`, `*.spec.tsx`, `tests/e2e/specs/`, `modal/*/tests/`) for the OLD form of every changed identifier or string. For each match found OUTSIDE the PR's diff: this is a stale reference. Flag as P0: 'Stale test reference: <file>:<line> still asserts OLD <string|signature> "<old-form>". The same PR must update this assertion — tests asserting old strings/signatures pass on main but fail (or hang) on the PR. CI surfaces failure 30+ min later, full rework cycle. Update in same PR; do not leave for follow-up.' Internal-only renames (private helpers not exposed beyond the file) are exempt — scope this gate to user-facing or public-API surface. Concrete check command: for each new/renamed user-facing string or exported identifier in the diff, run `grep -rn "<old-form>" tests/ __tests__/ 2>/dev/null` and confirm every match is either in the PR's diff or genuinely unrelated. Reference: PR #4085 (#4084 model-switch copy fix) — toast title was changed in `lib/editor/extensions/formatter-shortcut.ts` + `lib/utils/ai-operation-rollback.ts` but `tests/e2e/specs/inline-ai-rollback-toast.spec.ts:60` still asserted the OLD strings; CI E2E hung 30 min waiting for the old toast text that never appeared, full rework cycle. Anti-patterns this catches: toast title changed in component code with E2E spec still asserting old toast title; function arg renamed with unit test still passing old name; API response field renamed with integration test still expecting old field; validation message reworded with vitest still asserting old text. (16) E2E SPEC PLACEMENT (HARD RULE per Rajiv directive 2026-05-06 20:39 IST): Diff-grep gate before approving any PR — run `git diff origin/main...HEAD --name-only --diff-filter=A | grep -E '^tests/e2e/specs/[^/]+\.spec\.ts$'`. If matches (i.e., new spec at top-level under `tests/e2e/specs/`, NOT under `core/` or in `qa-tests/`), flag as P0 REJECT unless the PR body explicitly justifies why this spec must run on every CI gate. Suggest: move to `tests/e2e/qa-tests/<spec>.spec.ts` (local verification) OR `tests/e2e/specs/core/<spec>.spec.ts` (after confirming happy-path gate is justified). Top-level placement is DEPRECATED post-#4063 (lean E2E reorg) — `e2e.yml:469` still globs `tests/e2e/specs/` recursively, so any top-level spec accidentally gates CI. Reference: PR #4111 broke CI for ~3 hours by placing `template-style-geometry.spec.ts` (qa-only) at top-level. Paired with codex-app-plan-review CHECK 24. Both fire on the same drift. (18) SIBLING CALL-SITE SWEEP FOR MUTATED SHARED ARGS (HARD RULE per Codex meta-analysis 2026-05-16, Rajiv-approved 16:54 IST): When the diff modifies how a shared function is called (e.g., removes `&& args.userId` gate, adds new arg, changes a conditional that controls an arg literal), grep ALL OTHER call sites of the same function in the diff's reachable scope. Flag as P1 if ANY sibling call site is left with the older shape AND the divergence is observable downstream (attribution, status, audit, telemetry, persisted field). Source: PR #4582 `r3252693749` + `r3252635403`. (19) CLEANUP-SYMMETRY ON RELAXED EARLY-GUARD (HARD RULE per Codex meta-analysis 2026-05-16): When the diff relaxes an early validation guard such that the new fall-through path runs AFTER an external side-effect (R2 upload, Convex insert, Modal dispatch, S3 write, third-party API call), the diff MUST include cleanup of the side-effect on every new early-return / throw / catch path. Flag as P1 if any side-effect-producing line precedes a new return without corresponding cleanup. Source: PR #4581 `r3252615016` + `r3252683032`. (20) PROGRAMMATIC-RESET AUDIT FOR NEW BOOLEAN STATE FLAG (HARD RULE per Codex meta-analysis 2026-05-16): When the diff introduces a new boolean flag tracking user intent (e.g., `userTitleEdited`, `manuallyOverridden`, `userHasDismissed`, `formDirty`), grep all sites that mutate the underlying tracked value programmatically (resets, switches between modes, server-driven refills, derived auto-population). Each programmatic mutation MUST also reset the flag. Flag P2 with `<file>:<line>` of every site missing the reset. Source: PR #4581 `r3252701614`. (21) TELEMETRY/OBSERVABILITY SYMMETRY ON PAIRED BRANCHES (HARD RULE per Codex meta-analysis 2026-05-16): When the diff adds telemetry to ONE branch of an if/switch/Promise/onClick where sibling branches represent equivalent user actions, sibling branches MUST emit the same telemetry (with action-discriminating field). Flag as P2 if any sibling branch performs the same user-observable action without emitting the matching telemetry event. Source: PR #4557 `r3247827519`. <!-- Codex meta-analysis 2026-05-21 PR #4833 trigger: null-is-ambiguous diagnostic return on callback noop fence --> (22) NULL-IS-AMBIGUOUS DIAGNOSTIC RETURN ON CALLBACK HANDLER NOOP FENCE (HARD RULE per Codex meta-analysis 2026-05-21 PR #4833 trigger): When the diff adds a stale-callback noop fence (e.g., webhook handler returns 200 + `{noop:true}` based on a diagnostic mutation/query returning `null`), `null` may mean both (a) the intended stale-runId case AND (b) auth failure on a legitimately-active transcript with `pipelineRunId = null` (shared-secret mode, missing context, lookup failure). The fence then misclassifies auth failures as stale callbacks and Modal exits cleanly without retrying — silently dropping callback delivery. Flag as P1: 'Null-result triggers noop on `<file>:<line>` — null is ambiguous (stale-runId vs auth-failure-on-null-runId vs lookup-failure). Distinguish via explicit return shape from the diagnostic helper: `{stale: true}` / `{authFailure: true}` / `{notFound: true}` — never collapse to bare null. The handler must only emit `200/noop` on the `stale: true` discriminant.' Concrete check: grep the helper's return sites — does it ever return `null` for non-stale reasons? If yes, the noop fence will misclassify. Source: PR #4833 `r3280583373` on `convex/http.ts:430`. <!-- Codex meta-analysis 2026-05-21 PR #4833 trigger: file-age-only liveness check for lock/sentinel reclaim --> (23) FILE-AGE-ONLY LIVENESS CHECK FOR LOCK/SENTINEL RECLAIM (HARD RULE per Codex meta-analysis 2026-05-21 PR #4833 trigger): When the diff adds reclaim/cleanup of a lock-file, `.downloading` sentinel, `.tmp` intermediate, or any concurrency-coordination artifact based on file mtime alone (e.g., `mtime + GRACE > now → delete`), the diff MUST also include a liveness check confirming the original worker is dead — process check (PID file + `kill -0`), Modal task heartbeat, sentinel-content-with-startup-timestamp, or container-id verification. File age alone is unsafe: a long-running operation (preprocessing > GRACE, slow transcription, large-file ffmpeg) is indistinguishable from an abandoned operation. Flag as P1: 'File-age-only reclaim on `<file>:<line>` — long-running active worker can be misidentified as dead after `<GRACE_SECONDS>s`. Add liveness check (PID file + `kill -0 <pid>`, heartbeat timestamp embedded in sentinel content, container-id verification) OR raise GRACE conservatively past the longest legitimate operation time.' Concrete check: trace where the sentinel/lock is CREATED — does the writer embed a heartbeat/PID/container-id that the reader can verify? If only mtime, the reclaim is racy. Source: PR #4833 `r3280583378` on `modal/audio/processor.py:4336`. <!-- Codex meta-analysis 2026-05-24 PR #4981 trigger: runs-on migration tool-audit against runner image package list --> (24) RUNS-ON MIGRATION TOOL-AUDIT AGAINST RUNNER IMAGE PACKAGE LIST (HARD RULE per Codex meta-analysis 2026-05-24 PR #4981 trigger): When the diff changes the `runs-on:` value for any GitHub Actions job from `ubuntu-latest` (or another fully-provisioned GitHub-hosted runner) to a custom self-hosted runner label (e.g., `[self-hosted, modal, modal-light]`, `[self-hosted, hetzner, warm]`, `modal-perf`, etc.), the reviewer MUST audit every shell step inside that job for tool dependencies that the GitHub-hosted image provides for free but the self-hosted runner image may NOT install. Open the runner's image definition (`modal/ci-runner/app.py` for Modal runners; equivalent Dockerfile / cloud-init for Hetzner) and inspect the package list. For each binary invoked in the job's `run:` block (e.g., `gh`, `aws`, `docker`, `bun`, `npx`, `python3`, `jq`, `git`, `curl`, `node`, language-specific compilers, language-version managers), confirm it appears in the self-hosted image's installed-packages list. Flag as P1 (or P0 if the job gates merges, deploys, or critical PR flows): 'runs-on migration on `<file>:<line>` invokes `<tool>` but `<runner-image-source>` does not install it. `<tool>` exits 127 (command not found), job hard-fails, blocks <merges / deploys / PR gate>.' Concrete check command: `grep -E "RUN apt|RUN pip|RUN apk|RUN brew install|image=.*from-registry|.pip_install|.apt_install" <runner-image-source>` and compare against `grep -oE '\b(gh|aws|docker|jq|bun|npx|node|python3|curl)\b' <changed-workflow>.yml`. Common gap: GitHub `ubuntu-latest` ships `gh`, `aws`, `docker`, `jq`, `node`, `python3` pre-installed; Modal `modal-light` image typically ships `curl/git/jq` ONLY. Anything else MUST be either (a) added to runner image package list in the same PR, (b) installed inline via `run: apt-get install -y <tool>` step prepended to the job, or (c) the job left on `ubuntu-latest` with explicit `# keep on hosted: needs <tool>` comment. Reference: PR #4981 Codex bot inline `r3294842000` — migration of `detect-docs-only` to `modal-light` would have shipped without `gh api` availability, breaking docs-only-PR gating until next deploy. Fixed pre-merge by reverting `detect-docs-only` runs-on. See `~/.claude/projects/.../memory/feedback_gha_to_self_hosted_runner_migration_must_audit_job_tool_usage_against_runner_image_package_list.md`. (25) CUSTOMER ARTIFACT DELIVERY INVARIANT (HARD RULE per Rajiv CTO directive 2026-05-26 10:57 IST thread `1779763828.526989`): For auto-process, proofread, format, BoN, validator, and export paths, the product default is to deliver the best usable content-safe output and surface warnings. Do not hard-reject, roll back, block draft readiness, or discard a non-empty usable candidate because of validator uncertainty, score thresholds, formatting drift, co-occurrence ratios, or quality concerns. Those must become structured warnings and `ready_with_warnings`. Hard reject is allowed only when delivery is impossible or unsafe: no candidate exists, persistence/export is impossible, auth/security/privacy fails, or the artifact would be materially corrupt in a way the user cannot reasonably inspect/recover from. Any threshold controlling validator severity, BoN rescue, warning vs hard block, or candidate selection must be configurable through the existing Modal BoN/validator threshold config. New magic-number thresholds are a review blocker. **Code review gate shape**: P0/P1 on any NEW `hard_reject`, `valid:false`, `operation_success:false`, or fallback/original-text path that discards a non-empty content-safe candidate. Existing code with these patterns is grandfathered IF tagged with a "legacy reject path — does not apply to warn-not-reject default" comment; new code is NOT. Flag as P0 (or P1) any NEW magic-number threshold that controls validator severity / BoN rescue / candidate selection — these MUST be wired through the existing Modal BoN/validator threshold config, not hardcoded. Concrete check: grep diff for `hard_reject`, `valid:\s*false`, `operation_success:\s*false`, `fail_open_to_`, `return None  # discard`, `raise.*Validation`, new constants in `pipeline_guards.py` / `processor.py` / `validators/`. For each match: is the discarded candidate non-empty content-safe? If yes, P0 reject — re-shape as soft warning + `ready_with_warnings`. <!-- Codex meta-analysis 2026-05-26 PR #5028 trigger: synchronous observability post on hot pipeline return path --> (26) SYNCHRONOUS OBSERVABILITY POST ON HOT PIPELINE RETURN PATH (HARD RULE per Codex meta-analysis 2026-05-26 PR #5028 trigger): When the diff wraps a hot pipeline return path (rung-N BoN candidate, fallback chain rung, proofread/format/SC stage return, any Modal-side function on the wall-time-budgeted path) with a synchronous observability call — `RunTree.post()`, `langsmith.create_run()`, `wandb.log()`, `httpx.post()` to an analytics endpoint, `requests.post()` to LangSmith / Datadog / PostHog, OR any network I/O whose latency is added to the path's wall-clock — the diff MUST use the same async/bounded-timeout pattern that the file already uses elsewhere for the same call. `try/except` is fail-open for ERRORS but is NOT a latency hazard mitigation — slow/unreachable backends still block the return path while network I/O is in flight, pushing the flow past wall-time budgets. Flag as P1: 'Synchronous observability post on `<file>:<line>` blocks the return path while network I/O is in flight. The same file uses `<existing-async-or-timeout-pattern>` for `<same-call>` at `<line>` — follow that pattern (5s thread timeout, `asyncio.create_task`, `threading.Thread`, `concurrent.futures.ThreadPoolExecutor`, fire-and-forget queue, or bounded-wait wrapper).' Concrete check: when the diff adds `<observability-call>(...)` on a return path inside `modal/`, grep the SAME file for prior usages of the SAME call (`grep -n "RunTree\\.post\\|langsmith\\|wandb\\.log" <file>`). If a prior usage applies a timeout/thread/async wrapper and the new usage does not, P1 reject — observability latency must not stall the pipeline. Reference: PR #5028 Codex bot inline P1 — `modal/audio/processor.py` chunked rung-5 path wrapped `RunTree.post()` in `try/except` (fail-open for errors) but the same file's prior LangSmith post pattern uses a 5s thread timeout (around line 21256). Slow/unreachable LangSmith backend in `LANGSMITH_ENABLED` mode would stall rung-5 chunked completions and push proofread flow past wall-time budgets before `combined` is returned. See `~/.claude/projects/.../memory/feedback_synchronous_observability_post_on_hot_pipeline_return_path_must_use_existing_async_or_bounded_timeout_pattern.md`. <!-- Codex meta-analysis 2026-05-29 PR #5128 trigger: surface-X-downstream fix wrote to a plausible sibling field, not the field the consumer derives from --> (27) SURFACE-X-DOWNSTREAM FIX MUST WRITE THE EXACT FIELD THE CONSUMER DERIVES FROM (HARD RULE per Codex meta-analysis 2026-05-29 PR #5128 trigger): When the diff claims to address a "surface X downstream" / "report X as a warning" / "route X to status Y" finding — i.e. its stated purpose is to make an upstream signal (warn-band WER, severity class, degraded reason, quality flag) VISIBLE in a downstream DERIVED state (`draftReadiness.status`, `lastOperationSuccess`, `ready_with_warnings`, a Convex-derived label, a UI badge) — the reviewer MUST trace the written field all the way to the consumer's READ path and confirm the consumer actually derives the downstream state FROM that exact field. A fix that WRITES to a plausible-looking sibling field (e.g. `warn_fields`, `warn_ratios_out`, a telemetry-only dict) is NOT the same as a fix that reaches the consumer's derivation logic. Flag as P0/P1: 'Surface-downstream fix on `<file>:<line>` writes `<field>` but the consumer derives `<downstream-state>` from `<actual-field/reason>` — repo-wide `rg <field>` shows no consumer reads it (or only a telemetry sink reads it). The signal stays telemetry-only / single-pass-only; the downstream state never changes.' Concrete check: (a) grep the written field repo-wide (`rg <field>`) — if NO consumer outside the producer reads it, the fix is inert; (b) find the downstream state's actual derivation site (e.g. Convex `operationReason:validator_warning:*` → `ready_with_warnings`, the webhook translation `modal/...operation_success` → Convex `lastOperationSuccess`) and confirm the written field is on THAT read path; (c) require a WIRE/end-to-end test asserting the downstream DERIVED state (`draftReadiness === "ready_with_warnings"`), NOT a unit test asserting only the field-write — a unit test asserting the write passes while the downstream state is never derived. Also verify the fix covers ALL producer paths (single-pass AND aggregate/chunked), not just one. Reference: PR #5128 Codex bot inline P2 `modal/audio/speaker_correction_policy.py:394` — warn-band spoken-WER was added to `warn_fields` and described as "warn_fields propagated downstream", but `rg warn_fields` showed no consumer, Convex derives `ready_with_warnings` from `operationReason:validator_warning:*`, and the aggregate path propagated nothing → warning threshold stayed invisible in production. Companion: feedback_pm_merge_ready_must_verify_fix_reaches_consumer_derivation_not_just_writes_a_field. (42) PLAN-TO-IMPLEMENTATION DRIFT GATE FOR PIPELINE/ROUTING/FALLBACK CHANGES (HARD RULE per #5079 / PR #5085 retro 2026-05-28): PLAN CONTENT: [paste plan content if present]. When the PR touches `modal/audio/processor.py`, `modal/shared/pipeline_guards.py`, `modal/shared/pipeline_validators.py`, ASR provider selection, SC/proofread/format routing, BoN ladders, fallback chains, validator severity, terminal/fail-open behavior, or direct model-provider integration, compare the diff and tests against the issue body AND the plan's executable AC/test contract. The reviewer must not accept helper-only or classifier-only tests for behavior owned by a production orchestration path. For each planned routing/fallback AC, require a test that calls the production entrypoint (for example `_run_speaker_correction_stage`, `GuardedLLMStage.run`, the ASR provider selector, or the proofread/format stage runner) plus a negative-route proof. REQUEST_CHANGES if any of these are missing: (a) production entrypoint test for the route/fallback AC; (b) negative provider/layer proof, such as no unintended Layer 1 retry, no OpenRouter fallback-of-fallback when direct OpenAI is required, and no Gemini path reuse; (c) explicit terminal/fail-open status rather than identity/unchanged-text sentinels; (d) REJECT_TERMINAL and chunked acceptance rescue evidence when those paths are in the plan; (e) normal, insufficient, absent/zero deadline, and per-draw remaining-deadline tests for budgeted loops; (f) direct model clients mocked in unit tests with no live `api.openai.com`, Gemini, OpenRouter, WhisperX, or AAI calls; (g) plan AC ledger or PR checklist updated from planned to verified with exact test names; (h) clean PR file scope. A PR that implements different behavior than the accepted plan must say so explicitly and get PM/architecture approval; silent drift is REQUEST_CHANGES. <!-- Codex meta-analysis 2026-05-29 PR #5129 trigger: vacuous regression test (mock-only / signature-only / no-routing-assertion) --> (27b) CONTRACT-PRESERVING EXTRACTION GATE (HARD RULE per #5264 / PR #5298 retro 2026-06-05): When the diff extracts, wraps, renames, or moves a helper that crosses a Modal/Convex/R2/webhook/state-machine boundary, compare behavior against origin/main before approving. Check outbound payload fields (names/casing/null omission/auth/timeout/endpoint), return shape consumed by every caller (wrapper vs unwrapped, key lookup, truthiness, sentinel identity, `in` checks), retry/error/noop/cancel/404 branches, and drain/order semantics. REQUEST_CHANGES if any caller contract changes without explicit issue approval and old-bug-replay tests. Boundary tests must capture the actual emitted payload, not only kwargs or diagnostic dicts. Trigger: #5298 extracted `_post_pipeline_update`; early heads changed v2 POST fields and finally returned the full response wrapper instead of unwrapped `value`, disabling the `start` transcriptId mismatch guard. (28) VACUOUS REGRESSION TEST GATE (HARD RULE per Codex meta-analysis 2026-05-29 PR #5129 trigger): A regression test that CLAIMS to guard a property X MUST exercise the REAL production path and ASSERT the consumer-observable outcome that property X guarantees. REJECT as P1 any regression test that: (a) MOCK-ONLY — mocks away the unit-under-test (the very function/stage whose behavior is being guarded is stubbed), so the assertion can never exercise the production code it claims to protect; (b) SIGNATURE-ONLY — asserts only a function signature / argument shape / call-arity with no invocation of the real path and no downstream-effect assertion; (c) NO-ROUTING-ASSERTION-ALONE — asserts only the ABSENCE of a routing/dispatch call (e.g. `expect(routeFn).not.toHaveBeenCalled()`, `assert mock_router.call_count == 0`) WITHOUT also asserting the positive consumer-observable result the property guarantees — a no-call assertion alone passes even if the guarded property silently regressed via a different path. The test must drive the real path and assert the downstream effect a user/consumer would observe (output content, derived state, persisted field, exported artifact), not a structural proxy. Flag as P1: 'Vacuous regression test on `<file>:<line>` — <mock-only|signature-only|no-routing-assertion>; does not exercise the real path / asserts a structural proxy not the consumer-observable outcome. Rewrite to call the production entrypoint and assert <the actual guarded property>.' Concrete check: for each test the PR adds/labels as a regression guard, confirm the unit-under-test is NOT mocked, the production entrypoint IS called, and the assertion targets an observable result — not just `.toHaveBeenCalledWith(...)` / `.not.toHaveBeenCalled()` / a type-shape match. Reference: PR #5129 CTO REVISE (2 rework rounds) — `modal/audio/tests/test_format_proceedings_chunking.py:2088` was mock-only, `:2118` asserted no-routing-call only, `:2140` asserted a signature only; all flagged vacuous. (LLM PROXY / STT FIXTURE DETERMINISM GATE — HARD RULE, PR #5140 retro 2026-05-30): If the diff touches `modal/shared/llm_proxy_server.py`, WhisperX/AssemblyAI/TSVAD payloads, ASR/STT fixture hashing, hotwords, proxy-only metadata, fixture capture, or strict replay, flag P0/P1 unless the PR proves: same canonical decoded audio content creates the same STT fixture key across dynamic project/transcript IDs, filenames, presigned URLs, and container encodes; different audio creates a different key; hash input excludes raw container bytes, URLs, filenames, project/transcript IDs, timestamps, and hotwords; proxy-only metadata such as `source_audio_digest` is included in proxy normalization and stripped before upstream calls; all sync/callback/fallback entrypoints share the payload builder or are proven unaffected; fixture tests fail closed on repo fixtures with no `~/Downloads` fallback or skip; tests exercise production proxy path or TestClient+upstream spy, not copied helper logic; audio decode/hash streams/chunks subprocess output rather than `capture_output=True` over whole PCM; readiness requires latest-head capture plus latest-head strict replay. Capture green alone is APPROVE_PENDING_CI at best. VERDICT: APPROVE, REQUEST_CHANGES (with P0/P1/P2 categorization), or REJECT (with reason). P0 = must fix before merge. P1 = should fix, blocking. P2 = nice to have.
## Architecture-to-implementation consistency (mandatory for every PR)

Independently verify that the current diff preserves the authoritative
capability, production case, runtime trace, approved control point, and split
decision.

For every behavior-changing file, map:

production case → entrypoint → runtime path → changed control point →
consumer-visible outcome → regression proof.

REQUEST_CHANGES when:
- the diff changes a secondary or adjacent path instead of the owning control
  point;
- the affected production case does not reach the changed code;
- the test bypasses the changed production path;
- rework introduces behavior outside the approved scope;
- a previously excluded control point is added to the PR;
- repair and forward-prevention behavior at different control points are
  combined.

Set TERMINAL_DISPOSITION: ARCH_REVIEW_STALE, and stop ordinary code review,
when:
- the implementation changes the approved runtime control point;
- a Rajiv directive or rescope changes the intended capability;
- the PR now requires a different service, state transition, persistence
  boundary, or affected population;
- the diff contains multiple independently shippable control points that should
  be split.

Do not issue another ordinary rework packet for an architecture-stale PR.

## Structured rescope evidence (mandatory for every PR)

The GitHub issue is the authoritative scope contract. For any split, deferral,
narrowing, runtime-control-point change, or intended-capability change, require
the issue body or an authoritative issue comment to record the original
requirement, exact new scope, type/reason, concrete
runtime/test/artifact/dependency evidence, owner, linked follow-up issue for
every deferred/split item, and named approver. A Rajiv-directed capability
change requires explicit Rajiv approval. The PR description must summarize the
rescope and link to the issue record; PR-only wording is invalid.

REQUEST_CHANGES and set `TERMINAL_DISPOSITION: ISSUE_REWRITE_REQUIRED` while the
issue still promises the old work. If the recorded rescope changes the approved
architecture contract, set `ARCH_REVIEW_STALE` after the issue is corrected.

Return these exact fields:

RESCOPE_STATUS: NONE | PROPOSED | RECORDED
RESCOPE_TYPE: none | split | defer | narrow | control_point_change | capability_change
ORIGINAL_REQUIREMENT: <original issue/directive requirement, or none>
NEW_SCOPE: <replacement scope, or none>
RESCOPE_REASON: <why the scope changed, or none>
RESCOPE_EVIDENCE: <runtime/test/artifact/dependency evidence, or missing>
RESCOPE_OWNER: <named owner, or missing>
FOLLOW_UP_ISSUE: <linked issue, none, or missing>
RESCOPE_APPROVER: <named approver and evidence, or missing>
ISSUE_CONTRACT_UPDATED: yes | no | not_applicable
PR_SUMMARY_UPDATED: yes | no | not_applicable

### Review convergence

Assign every blocking finding a stable class and ID: DIRECTIVE, CONTROL_POINT,
REACHABILITY, SCOPE, IMPLEMENTATION, or PROOF.

On re-review:
- first state whether each prior blocking finding is resolved;
- introduce a new blocker only when it was caused by the revision or could not
  previously be inspected;
- do not restate the same concern under a new description.

If DIRECTIVE, CONTROL_POINT, REACHABILITY, or SCOPE remains blocking for two
rounds, stop ordinary review, set VERDICT: REQUEST_CHANGES, and return exactly
one terminal disposition:

TERMINAL_DISPOSITION: ARCH_REVIEW_STALE | SPLIT_REQUIRED |
ISSUE_REWRITE_REQUIRED | PM_ADJUDICATION_REQUIRED

Do not request a third ordinary revision for the same architectural class.

<!-- Codex meta-analysis 2026-06-01 PR #5173 trigger: Python duplicate-module import/test-order determinism -->
(29) PYTHON TEST IMPORT-ORDER DETERMINISM GATE (HARD RULE per Codex meta-analysis 2026-06-01 PR #5173 trigger): When the diff adds or modifies Python tests under `modal/**/tests/` or any test that mutates `sys.path`, imports a bare module name shared by multiple directories, or patches by string path (e.g. `from processor import ...`, `mock.patch("processor...")`), the reviewer MUST check for duplicate module basenames in the repo (`rg --files | awk -F/ '{print $NF}' | sort | uniq -d`) and verify the test binds deterministically to the intended module. In this repo, both `modal/audio/processor.py` and `modal/docx/processor.py` exist, so bare `import processor` is order-dependent. Flag P1 if a new/changed test relies on `sys.path.insert` + bare import or string patch where a sibling test can preload the other module. Required fix: import by absolute file path or package-qualified module into a unique `sys.modules` name and patch with `mock.patch.object(<that_module>, ...)`; then run an order-sensitive repro combining the changed test with a sibling test that imports the competing module. CI green on the isolated file is not enough.

LONG-FILE EDITOR RESPONSIVENESS CONTRACT (HARD RULE per Rajiv CTO directive 2026-07-20): Treat `tests/e2e/specs/core/smoke-large-file-perf.spec.ts` with `SEED_PROJECT_LONG` as a release-critical baseline against user-visible main-thread lag. Trigger by runtime reachability/file/symbol, not PR wording, when the diff touches `components/editor/extensions/CSSPagination/**`, `hooks/use-audio-highlighting.ts`, `hooks/use-transcript-editor.ts`, `lib/editor/stores/WordTimingStore.ts`, `lib/editor/store-reconciliation.ts`, `lib/editor/position-index-lifecycle.ts`, `lib/editor/audio-time-transport.ts`, or ProseMirror/bulk-edit flows feeding them. REQUEST_CHANGES for interaction-time synchronous full-DOM scans, full-document LCS, O(n) remaps, large index builds, per-tick React/ProseMirror/pagination churn, regression of debounce/rAF/idle/worker/dirty-region/mark-only/bulk-barrier controls, main-thread fallback, disabled instrumentation overhead, or threshold-as-fix. Preserve representative long-file load, editing, scrolling, playback/seek/highlighting, and concurrent editing+playback; affected windows must assert bounded long tasks/responsiveness. Missing telemetry, undersized seed, unavailable required audio, or skipped/conditional scenarios must fail closed. Threshold relaxation requires same-seed/same-runner baseline evidence plus explicit CTO approval. Manual/local/qa proof is supplemental. Pure prerequisites may delegate only to a named runtime issue retaining the guard. Return exact fields: `EDITOR_PERF_GUARD: NOT_TRIGGERED | PASS | REVISE | REJECT`; `HOT_PATHS`; `AFFECTED_SCENARIOS`; `MAIN_THREAD_CONTROL_POINTS`; `BASELINE_EVIDENCE`; `THRESHOLD_CHANGE: none | approved | unapproved`; `FAIL_CLOSED_PROOF`.

UNIT-TEST DETERMINISM GATE (HARD RULE per branch-green/main-red meta-analysis 2026-07-29): Trigger when the PR adds or modifies a unit/integration test using timers, requestAnimationFrame, microtasks, wall-clock/performance timestamps, randomness, workers, listeners, concurrent tasks, filesystem/network completion, or asynchronously produced collections/events. Inspect the test harness itself. REQUEST_CHANGES when correctness depends on a fixed sleep, default timeout, uncontrolled timer/rAF ordering, wall-clock threshold, unchecked `[0]` lookup, retry wrapper, or one green CI run. A fixed sleep may simulate elapsed time but cannot synchronize an assertion. Require an observable production completion boundary (promise/callback/event/latch); captured and explicitly released timer/rAF callbacks plus one adverse interleaving when ordering is under test; afterEach cleanup of timers/listeners/performance entries/mocks/global state; functional proof on small deterministic fixtures separate from real wall-time benchmarks; and five consecutive targeted executions under normal CI worker settings. A timeout increase alone is not a correctness fix, and repetition never substitutes for explicit synchronization. Return exact fields: `TEST_DETERMINISM: NOT_TRIGGERED | PASS | REQUEST_CHANGES`; `RISK_PRIMITIVES`; `SYNCHRONIZATION_BOUNDARY`; `ADVERSE_INTERLEAVING_PROOF`; `TARGETED_REPEAT_PROOF`.

<!-- NO_DISABLED_BUTTON_UI_RULE_V1 -->
UI RULE (Rajiv 2026-09-26): Do not gate actions with disabled buttons. Buttons stay enabled; on click, validate and render an error state on the offending control (checkbox/field error styling + inline message) and do not proceed. The only allowed disabled state is the action's own in-flight/double-submit guard. Plans and reviews must call out any new `disabled=` on a button that encodes a precondition and require the error-state pattern instead.
NO-DISABLED-BUTTON GATE: REQUEST_CHANGES and name `NO-DISABLED-BUTTON GATE` when the diff adds a precondition-gated disabled button (e.g. `<Button disabled={!agreed}>`, `disabled={!isValid}`); require an enabled button, click-time validation, `aria-invalid` + inline error text on the offending control, no action on invalid click, and a test that clicks the enabled button with the precondition unmet. `disabled={isSubmitting}`/`disabled={isPending}` for the action's own in-flight guard is allowed.

<!-- REVIEW_CONVERGENCE_POLICY_V1 -->
CONVERGENCE CONTRACT: Verify the production entrypoint and owning control point
before applying specialized gates. Inspect the changed path and its named
downstream consumer seams; do not expand into unrelated speculative review.
Every P0/P1 must emit a stable blocker record. P2 is non-blocking unless
`SEVERITY_OVERRIDE: P1` and an evidence-bearing override reason are emitted.

<!-- BEHAVIORAL_AC_PROOF_CONTRACT_V1 -->
BEHAVIORAL AC PROOF CONSUMPTION: For every behavior-changing AC, verify the
production case reaches the owning runtime control point and the predeclared
exact local command proves the expected observable plus the old-bug/adverse
case using deterministic evidence. Missing proof is one stable bounded PROOF
or REACHABILITY blocker. On the repeated same-class finding or review cap,
return one terminal patch/rescope/split/override/product-decision disposition;
do not request another generic review round.

 (29) VALIDATOR HARD-REJECT RECURRENCE GUARD (issue #7944 lineage — a validator string is not a corruption witness): When the diff touches Modal validators, severity mapping, BoN candidate selection/fallback, exception mapping, readiness derivation, or any `hard_reject`/`catastrophic`/`valid:false`/forced-original-fallback path, apply this negative example. Production cover formatting failed with `cover_structure_contract:missing immutable anchor: jurisdiction` — that phrase alone proves neither content corruption nor whether the source contained the information. Executed discriminators on main `6793164e`: control `STATE OF NEW YORK` newline `DEPOSITION` is valid; blank-line-only insertion yields `output does not match one unambiguous declared structure`; `DEPOSITION`→`Deposition`-only yields `missing immutable anchor: proceeding` — no content deleted in either, yet `processor.py` promoted both to catastrophic/hard failure. Flag as P0 any code that: rejects a usable candidate (or retries solely, forces original fallback, or fails readiness/pipeline completion) over a heading/template/blank-line/casing preference; treats a validator literal or ratio as a corruption witness; or re-promotes a soft/warning result to rejection downstream (a re-promoted soft result is still a must-fix defect). Correct shape: deterministic existing DSL rule normalizes COVER HEADING to `DEPOSITION` (heading-only, idempotent); missing metadata and benign wording/casing/layout differences become structured warning logs with usable output selectable and deliverable (`ready_with_warnings`); no new popup/status schema. Still hard-reject demonstrated content corruption with a concrete witness (word-level LCS + deleted span), and do not weaken auth, parseability, transport, save CAS/version, or loss/duplication safeguards.

## Runtime and branch coverage (CTO directive Ev0C3ECXCGJF)

For changed production paths, identify the actual execution runtime, verify new imports/APIs and their
dependencies are supported there, and verify the test enters the changed critical branch with
production-shaped state. A mock or a test that bypasses that branch is not runtime-compatibility proof.

Code-reviewer specifics:

- Inspect new runtime dependencies and their reachable call paths.
- Reject concrete unsupported APIs/imports in the ACTUAL runtime - specifically runtime dynamic loading
  reachable from Convex queries/mutations. Prefer static imports for that shape; `node` cannot make a
  query/mutation Node-capable.
- Do NOT ban valid type imports, test modules, or genuinely supported Node/action-only code.
- Shared production code remains production-impacting even when the parent feature says "test mode".
- Flag concrete runtime harm, not paperwork.
