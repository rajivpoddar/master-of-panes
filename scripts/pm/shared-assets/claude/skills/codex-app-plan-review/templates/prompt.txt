Review this implementation plan against the GitHub issue. PLAN: [paste plan content] ISSUE: [paste issue body] TOP-LEVEL NOTE — E2E COVERAGE SCOPE (Rajiv directive 2026-05-02 13:55 IST + 2026-05-09 21:41 IST, `feedback_e2e_happy_path_only.md`): E2E assertions are REQUIRED ONLY for happy-path on CORE BUSINESS FUNCTIONALITY: (a) Editor interactions (TipTap/ProseMirror typing, paste, formatting, dark mode, toolbar, version history); (b) DOCX export path (template render, OOXML structure, sectPr, headers/footers, page breaks, line numbering); (c) Auto-process pipeline happy-path (upload → ASR → speaker_correction → enhance → format → ready terminal state). For EVERYTHING ELSE, do NOT demand E2E assertions. Unit + integration + Python wire tests are SUFFICIENT and CORRECT: edge cases / error paths (validators, fences, warnings, retries, fallback branches, interpreter shutdown, ContainerRestartRequired, SSE disconnect, error-class taxonomy); infra / CI / workflow changes (e2e.yml, hooks, scripts); helpers, utilities, internal abstractions; observability (heartbeats, stage-update propagation, log emission); schema additions (with seed refresh per Phase 8b — but no E2E for the schema itself); refactors with no behavior change. If the issue body, plan, or PR description does NOT touch the editor, DOCX export path, or auto-process happy-path, do NOT REJECT the plan for missing E2E assertion — CHECKs 11 and 17 below are bypassed. Accept "unit + integration" or "Python wire test" as the canonical test lane. For changes that DO touch core business surfaces (a)/(b)/(c), E2E IS required and CHECKs 11/17 apply. Cap-at-3 escalation: if you flag the same E2E-scope concern across 3 plan-review rounds AND the slot/PM has cited the core-business classification (or pointed out the change does NOT touch editor/DOCX/auto-process), accept the override and APPROVE on the next round. PM has authority on scope per `feedback_codex_scope_disagreement_pm_override.md` — do NOT cycle past round 3 on scope disputes. Anti-pattern (caused 2 PM-OVERRIDE escalations 2026-05-09 on #4256 R4 + #4258 R3): rejecting a Python wire / unit / integration test plan for an interpreter_shutdown / ContainerRestartRequired / validator-revert fix because "no E2E assertion." That is the WRONG call — those fixes do NOT touch core-business happy-path; accept the canonical lane. CHECK: 1. Does the root cause match the user's EXACT reported scenario? 2. OUT-OF-SCOPE TRAP: Does the plan's Out of Scope section describe the user's actual bug? 3. Are all file paths and line numbers real (not guessed)? 4. For pipeline changes: is the full threading path documented? 5. For Convex changes: are mutations called from the correct context? 6. For Modal changes: is .env() vs secrets correct? 7. Does the test strategy include a FAILING test first (for bugs)? 8. Are there any missing steps? (e.g., missing trigger, missing cleanup, missing migration) 9. REQUIREMENT COMPLIANCE: Does the plan include a 'Requirements from Issue' section that quotes every explicit requirement from the issue body? Check for must/should/has-to statements, specific templates or artifacts mentioned, ordering constraints, and hard contracts. 10. For E2E test changes: does the plan include running the test locally with modal serve before PR? 11. E2E REGRESSION GUARD: If the plan modifies editor behavior (lib/editor/), pipeline behavior (modal/), DOCX export (modal/docx/processor.py), or user-facing performance — does the Test Strategy include adding an *output-shape ASSERTION* (not just running the existing test) to one of: auto-process-regression.spec.ts (DOCX export + editor render bug fixes), smoke-large-file-perf.spec.ts (perf), or errata-sectpr-regression.spec.ts (sectPr ordering)? CRITICAL: distinguish PATH coverage from ASSERTION coverage — if the existing test exercises the buggy code path but its assertions don't check the specific fix output (e.g., missing tab chars, missing w:firstLine value, drifted PROCEEDINGS heading position), a regression would silently pass. Plan MUST specify the exact assertion text/regex/structural check that distinguishes fixed-output from buggy-output. If not, **REJECT** (NOT REVISE): 'Plan must include an E2E assertion in tests/e2e/specs/auto-process-regression.spec.ts (or equivalent) that fails on the pre-fix code state. Unit tests are NOT a substitute. PM may not override with "Option B" / unit-only reasoning per Rajiv directive 2026-05-01 20:13. Specify the exact OOXML/DOM check that distinguishes fixed-output from buggy-output.' Reference: feedback_e2e_coverage_path_vs_assertion_distinction.md (PRs #3856, #3892, #3958 missed this — path covered, assertion absent, regressions invisible). 12. TYPE-SHAPE CONSUMER AUDIT (mandatory when the plan adds or modifies fields on a typed object that flows through React props or function args): Does the plan list a grep-based audit of every consumer of that type? For each consumer, classify as (a) passes-through (low risk), (b) re-shapes/slices into a new prop literal (HIGH RISK — must explicitly thread the new field), or (c) destructures specific fields (HIGH RISK). REJECT the plan if any HIGH-RISK site is missing from the change set. Example pattern (PR #3681 R1 bug): the create-template wizard `app/templates/new/page.tsx` slices the template object before passing it to `<TemplateLineSettingsSidebar>`, so any new `TemplateLineSettings` field must be explicitly added to that slice — a grep on the type alone misses this because it is a re-shaping boundary, not a direct caller. 13. UI DUPLICATION AUDIT (mandatory when the plan adds a new UI control to an existing settings panel, sidebar, form, or any container that already renders other fields): The plan MUST list every existing field/control rendered in the same parent container and prove the new control does not duplicate or overlap any existing concept. Two controls targeting the same user-facing setting (same DOCX property, same CSS attribute, same Convex field, same data binding) is a UX defect even if both stores work — the user does not know which one wins. REJECT the plan if it does not enumerate the panel's existing fields. Example pattern (PR #3681 R2 bug, found by Rajiv during manual test): plan added 'Cover Page' override to a NEW SECTION LINE SPACING list inside `template-editor-toolbar.tsx`, but the same Settings panel ALREADY exposed a top-level 'Cover Line Spacing (pt)' field. Both targeted the cover-page DOCX line-spacing property. Two visible controls for one property. Neither plan review nor code review flagged it because both reviews focused on the new code in isolation, not the rendered panel as a user would see it. 14. CONFIG-HOME ARCH FIT (mandatory when the plan adds or modifies a paragraph attribute, style geometry, or per-template setting emitted by the pipeline — alignment, indent, spacing, margins, font, color): Does the plan specify WHERE the value comes from? Classify into (a) hardcoded magic number — REJECT unless trivially derived from unit conversion (e.g. 72pt = 1in); (b) DSL var (formattingRulesYaml.vars) — only correct for text-rule config knobs (qa_label_period, whitespace detection width, label tokens) configuring a TEXT TRANSFORMATION; NOT correct for paragraph geometry; (c) Template style schema (template's section-style metadata) — correct home for paragraph geometry. For paragraph geometry attributes the plan MUST land on (c). If it proposes (a) or (b), REVISE with: 'Geometry value should live on template style schema, not as DSL var or hardcoded constant. Reference: #3799 broader formatting-rules-YAML migration owns this seam.' Example pattern (PR #3796 missed): plan hardcoded firstLineIndent: 144 (2") which broke Standard Depo (different geometry); rework re-parameterized to read from dsl_vars.get('proceedings_first_line_indent_pt') as second-best until template-style migration. 15. MULTI-TEMPLATE REGRESSION CHECK (mandatory when the plan modifies a code path that fires for multiple templates — pipeline formatter, transcript paragraph builder, DOCX exporter, editor plugins): Does the plan analyze impact on ALL templates hitting this code path, or only the template named in the issue? If only one template, REVISE the plan to enumerate other templates (Standard Depo, ACR-MA-MU, ACR-MA-Hearing) and verify the fix is correct for each, OR gate the change to fire only for the named template. Example (PR #3796): plan hardcoded firstLineIndent: 144 for ACR colloquy. Standard Depo has its own colloquy_indent rule — uniform 144 over-indents. Plan should have either parameterized the value or gated by template-id. 16. EMIT-SITE AUDIT (mandatory when the plan adds or modifies a field on a payload that crosses a service boundary — Modal endpoint POST body, Convex action args, webhook callback body, internal RPC arg, R2 object schema): The plan MUST list every parallel emit site that POSTs to or invokes the same downstream consumer, and confirm the new field is threaded through ALL of them — OR explicitly justify why each unfixed site is out of scope (legacy path being deprecated, etc.). REJECT or REVISE the plan if it touches one emit site but the audit reveals other unfixed parallel paths. Concrete check: for Modal endpoint changes, grep for every `fetch(MODAL_*_URL)` / `requests.post(.../endpoint)` / `internalAction` that hits the same endpoint; for Convex action arg additions, grep for every `fetchAction` / `ctx.runAction` / `useAction` / API route that invokes the same action; for webhook callback body changes, grep for every emitter of that callback shape. Example pattern (#3792 → #3815): #3792 added `playback_key` to the Modal `/proofread` endpoint payload. The fix threaded it through `convex/autoprocess.ts:1153` (auto-process path) but missed `app/actions/audio.ts:619-647` (manual proofread server action — user clicks proofread button), which POSTs to the same endpoint. Result: cache-miss `ValueError("audio_url required for cache-miss path")` still fires in CI on every manual proofread test. The fix shipped + passed QA + landed; the parallel emit site was never grepped. The plan review for #3778 should have asked: "what other code paths POST to /proofread?" — would have surfaced both emitters at plan time. 16b. CONTRACT-PRESERVING EXTRACTION AUDIT (HARD RULE per #5264 / PR #5298 retro 2026-06-05): If the plan extracts, wraps, renames, or moves a helper crossing a Modal/Convex/R2/webhook/state-machine boundary, REVISE unless it includes a before/after ledger against origin/main for outbound payload fields, return value shape, retry/error/noop/cancel/404 branches, drain/order semantics, and every return-value consumer. The plan must require a boundary-capture test for the actual emitted payload AND old-bug-replay tests for each non-obvious return consumer. Helper kwargs, diagnostic dicts, or direct serialization assertions alone are not sufficient. Trigger: #5298 extracted `_post_pipeline_update`; the wire-body test was right, but the extraction returned the full wrapper instead of unwrapped `value`, silently disabling the `start` transcriptId mismatch guard until review. 17. NO LONG-FILE PROOFREAD/FORMAT IN E2E (mandatory when the plan adds or modifies any test in tests/e2e/specs/ that triggers proofread (Cmd+Shift+P, /proofread endpoint, proofreadFullTranscript) or format (Cmd+Shift+F, /format endpoint, AI format) on a transcript larger than ~10k chars — including SEED_PROJECT_LONG, SEED_PROJECT_PROOFREAD, or any 100k+ char fixture): REJECT the plan. CI E2E is fixture-replay only — proofread/format on long files goes through real Gemini and blows the e2e timeout (~5-10min per attempt × 3 retries = 30+ min on a single test). Long-file proofread/format perf is verified by manual QA + production telemetry, not CI E2E. If the plan needs to verify proofread/format behavior in E2E, gate the test to a short fixture (heydonna.mp3, ~1k chars) OR skip the test outright. Reference: PR #3854 incident 2026-04-29 — `smoke-large-file-perf.spec.ts:733 Proofread large transcript perf (#3671)` triggered real proofread on Hazlehurst 5h depo every CI run, blew 20m→25m→35m→45m timeout budgets, blocked entire cascade-merge for hours. The test was removed; the rule prevents recurrence. 18. UI-STORAGE PARITY FOR YAML/CONFIG (mandatory when the plan modifies any value in `default_rules.yaml`, `formattingRulesYaml`, `dsl_vars`, template variables, or any user-configurable field that BOTH the pipeline reads AND the editor/template UI displays — including Variables panel, Settings panel, template editor sidebars): The plan MUST confirm that BOTH surfaces reflect the change identically after the fix. If the pipeline applies a runtime semantic transform (integer→tabs, space-count→tab-count, pt→twips, raw int→derived display) that the UI does NOT also apply when displaying the same field, REJECT the plan. The customer-facing UI is the authoritative view of "what this template does" — divergence breaks customer trust and QC verification (QCs read the UI to verify template behavior before approving auto-process output). The plan must answer: "If a QC opens the template editor for this template after my fix, will the displayed value match what the pipeline emits?" If no, the plan MUST either (a) update the UI rendering to apply the same transform, OR (b) migrate the storage shape so UI and pipeline read the same semantic without a transform (preferred — single source of truth). Reference pattern (PR #3856 → #3918 follow-up): #3856 added Modal-side runtime override converting `colloquy_tab=3` to `\t\t\t` (3 tabs) in exported DOCX. The plan never asked whether the Variables panel also needed to show "3 tabs" instead of "15 spaces (= 3 × 5)". Result: pipeline emits tabs, UI shows space-count — QC's authoritative view diverged from production output, broke their verification workflow. The fix was scoped half — required #3918 follow-up to converge UI + storage on tab-counts. Plan review should have caught this at scope time. 19. DESTRUCTIVE-DATA-OPERATION GATE (mandatory when the plan proposes any operation that overwrites user-mutable data — reseed scripts like `bun run seed:template --force`, full-table replace, full-document upserts, fixture-replace-all, full-template-overwrite mutations): The plan MUST justify why a **surgical** patch (in-place mutation of just the affected fields) is not possible. If the issue references a surgical path (`adminPatchX`, `migrateField`, `cleanupY`, "patch only Z", "or analogous") and the plan picks a broader destructive path, **REJECT** (NOT REVISE). Concrete pattern (PR #3946 #3942 incident 2026-05-01): issue said `adminPatchNYDepo* or analogous`; plan picked `bun run seed:template --force` (destructive — clobbers customer template customizations). Slot reasoned `adminPatch` was wrong because "highlights are in DOCX binary, not JSON text" — which is false; the highlight markers live in `templatedJson` (the JSON field on `docxTemplates`) exactly where `adminPatchNYDepo*` operates. The plan's destructive choice was based on a false premise about where the data lives. REJECT verdict text: "Plan substitutes destructive operation for surgical path the issue specified. Re-derive: where does the affected data physically live? If in `templatedJson` / a Convex field, `adminPatchX` is the right shape. Reseed only when the data being changed is in the source binary (DOCX file itself) AND that binary is uploaded fresh AND no per-customer customization on top of it is at risk." 20. SCHEMA CHANGE → SEED REFRESH (HARD RULE per Rajiv 2026-05-05 11:51 IST): If the plan touches `convex/schema.ts`, `convex/migrations.ts`, or any file matching `convex/migrations/*.ts` or `convex/migrations*.ts`, the plan MUST include a **Phase 8b: Refresh preview-seed.zip** step AFTER the Codex QA review phase and BEFORE the final STOP/mark-ready step. Phase 8b must include the export command `env -u CONVEX_DEPLOY_KEY CONVEX_DEPLOY_KEY="$(grep '^CONVEX_DEV_DEPLOY_KEY=' .env.local | cut -d= -f2-)" npx convex export --path tests/e2e/fixtures/preview-seed.zip --include-file-storage` followed by a `git add tests/e2e/fixtures/preview-seed.zip && git commit && git push` step so CI re-runs against the refreshed seed. **Verdict: REVISE** if the schema-touching plan omits Phase 8b. Override hatch: if the schema change is genuinely seed-irrelevant (e.g., pure index addition on an empty table, comment-only edit), the plan must include a one-line note "Seed refresh skipped: <reason>" — accept the override if reasonable. Rationale: per-PR Convex preview infra (#3986) seeds from `tests/e2e/fixtures/preview-seed.zip`; without refresh, schema-touching PRs leave the seed stale and downstream PR E2Es fail. See `tests/e2e/fixtures/README-preview-seed.md`. 21. ERROR/WARNING COPY PRODUCT-MEMORY CHECK (mandatory when the plan touches `app/api/format/route.ts`, `app/api/proofread/route.ts`, `app/actions/formatting.ts`, or any error/warning string surfaced on the proofread or format paths): Search the plan's proposed copy / error message strings for partial-selection advice — `"select a smaller section"`, `"select smaller text"`, `"try a smaller selection"`, `"reduce the selection"`, `"shorter selection"`, or any phrasing that tells the user to make their selection smaller. PRODUCT MEMORY: HeyDonna's proofread + format paths are ALWAYS full-section operations — the user does not select a range; the operation always runs on the entire current section. Only the edit-selection-with-AI feature operates on a partial selection. Suggesting "smaller section" on proofread/format is misleading because the user CAN'T reduce the section size — they need a different model. If the plan introduces or preserves such copy, REJECT/REVISE with: "Misleading error copy on full-section path. Replace with model-pivot guidance: 'AI model couldn't process this section. Try a different model in your settings.' or similar. Reference: proofread + format are always full-section (product memory). Only edit-selection-with-AI is partial-selection." This check is bidirectional: also reject the plan if it KEEPS existing misleading copy without rewriting it. Reference: Rajiv directive 2026-05-05 19:43 IST DM — `app/api/format/route.ts:691, :821` flagged for "Please select a smaller section of text" on a full-section path. 22. AFFECTED TESTS GATE (HARD RULE per Rajiv directive 2026-05-05 21:53 IST): When the plan modifies ANY of: (a) user-facing strings (toast title/description, error messages, button labels, alert text, modal copy, dialog content, validation messages, status badges, notification text); (b) function/method signature (added/removed/renamed args, return-type change); (c) public API endpoint shape (request/response field names); (d) schema field names or types; (e) public class/interface names exported from `lib/`, `app/api/`, `convex/`, `modal/`, the plan MUST include an Affected Tests step that, for EVERY changed identifier or string, greps across `tests/`, `__tests__/`, `*.test.ts`, `*.test.tsx`, `*.spec.ts`, `*.spec.tsx`, `tests/e2e/specs/`, and `modal/*/tests/` for the OLD form, lists each match, and updates the matching test files in the SAME PR. Internal-only renames (private helpers not exposed beyond the file) are exempt — scope this gate to user-facing or public-API surface. REJECT (P0) any plan that proposes changes to user-facing strings or public signatures without enumerating the corresponding test updates in the same PR. Verdict text on REJECT: 'Plan changes user-facing strings / public signatures but does not enumerate the test files asserting the OLD form. Tests asserting old strings pass on main but fail on the PR (or hang waiting for never-appearing UI). CI surfaces failure 30+ min later — full rework cycle. Required: grep tests/ + __tests__/ + *.spec.* for every changed string/identifier and update in same PR.' Reference: PR #4085 (#4084 model-switch copy fix) — toast title was changed in `lib/editor/extensions/formatter-shortcut.ts` + `lib/utils/ai-operation-rollback.ts` but `tests/e2e/specs/inline-ai-rollback-toast.spec.ts:60` still asserted the OLD strings; CI E2E hung 30 min waiting for the old toast text that never appeared, full rework cycle. Anti-patterns this catches: toast title changed in component code with E2E spec still asserting old toast title; function arg renamed with unit test still passing old name; API response field renamed with integration test still expecting old field; validation message reworded with vitest still asserting old text. 23. LLM PROXY IMPACT — async webhook handling (HARD RULE per Rajiv directive 2026-05-05 20:54 IST): For plans introducing new external API flows from `modal/` (Modal → Scribie/Gemini/WhisperX/LLM/any new outbound HTTP), classify the call shape: (a) sync request/response (e.g., POST → JSON returned in same call) — proxy intercepts + replays cached response, ✅ no plan change needed; (b) async webhook / callback URL pattern (e.g., POST → `{status: "pending"}`, separate webhook delivers result later) — `modal/shared/llm_proxy_server.py` CANNOT replay async deliveries because the external service never runs in proxy mode, so the callback is never fired, and the E2E test hangs at the workflow timeout. For (b), the plan MUST include a test-side callback synthesizer (helper that POSTs the synthesized callback body to `/api/<callback-route>` when `LLM_PROXY_ENABLED=true`) OR a Modal-side proxy-mode short-circuit (the Modal function detects proxy mode and emits the callback inline rather than via the external service). REJECT plans missing this analysis when the diff introduces an outbound call from `modal/`. APPROVE plans that explicitly address it with one of the two replay strategies. Reference: PR #3953 #3945 plan missed this — async webhook flow Modal → Scribie → /api/transcription-callback → Convex had no proxy replay path; smoke-cfr-audio-highlights test 1 hung at 25m budget on every CI run. 24. E2E SPEC PLACEMENT (HARD RULE per Rajiv directive 2026-05-06 20:39 IST): If the plan adds a new E2E spec file under `tests/e2e/`, the file MUST live in `tests/e2e/specs/core/` (happy-path / core regression — gates CI on every PR) OR `tests/e2e/qa-tests/` (qa-only — local verification, NOT run on CI). Top-level `tests/e2e/specs/<spec>.spec.ts` is DEPRECATED post-#4063 (lean E2E reorg) — `e2e.yml:469` still globs `tests/e2e/specs/` recursively, so any top-level spec accidentally gates CI. P0 REJECT if the plan places a new E2E spec at top-level without explicit justification stating why it must run on every CI gate. P0 REJECT if the plan adds a `tests/e2e/specs/core/<spec>.spec.ts` for an edge-case / error-path / validator behavior — that's qa-tests/ territory per `feedback_e2e_happy_path_only.md`. Default: if classification is uncertain, place in qa-tests/. Only place in core/ when testing PRODUCT-CRITICAL happy-path (auto-process, smoke, template-vars-fidelity). Reference: PR #4111 broke CI for ~3 hours by placing `template-style-geometry.spec.ts` (geometry-first style apply, qa-only) at top-level — picked up by `e2e.yml:469` glob, blocked merge. 25. MULTI-RUNG PIPELINE PARITY (HARD RULE per chitta 2026-05-09 BoN cascade analysis): If the plan introduces or modifies a candidate ladder (BoN / best-of-N), retry chain, fallback chain, or model-tier escalation (cand1 → cand2 → cand3 ...), the plan MUST explicitly verify that each rung has independent: (a) RESOURCE BUDGET — separate token / time / retry accounting per candidate; clusters or parent rungs MUST NOT silently share budget such that cand2 exhaustion blocks cand3 launch or vice versa; (b) ERROR CLASS ISOLATION — failures in one rung MUST NOT pollute another rung's result (e.g., cand1 ValueError caught in shared finally must not zero-out cand2 attempts_remaining); (c) MODEL-FAMILY-SPECIFIC QUIRKS — plan enumerates per-model adjustments: gpt-5.x `max_completion_tokens` cap (NOT `max_tokens`), gpt-5.x temperature limits, Sonnet 1M-context beta header, Flash Lite `thinking_config` suppression, OpenRouter audio-size limits; (d) EMPTY-OUTPUT POLICY — each rung explicitly declares whether empty output triggers HARD REJECT (advance to next rung) vs RETRY-IN-PLACE vs PROCEED-WITH-EMPTY; ambiguity here cascades into customer-visible bail; (e) PRE-FLIGHT VALIDATION — plan lists per-rung pre-flight checks that MUST run before invocation (ffprobe on audio inputs, tokenizer fit on prompt size, model availability ping); (f) FALLBACK GATE — if a caller-side fallback exists (e.g., Gemini fallback for cand2/cand3), plan must specify how each rung opts in/out via flag (allow_fallback_model or analogous) — silent caller-side fallback masks per-rung failure modes. REJECT verdict text on miss: 'Plan introduces multi-candidate ladder but does not enumerate per-rung resource/error/quirk/empty/preflight/fallback parity. Multi-rung pipelines without explicit per-rung accounting cascade into N follow-up bug-fix PRs as edge cases surface in production. Required: a per-candidate matrix in the plan covering all six axes (a)-(f). Reference: PR #4151 (BoN ladder Phase 2) cascaded 6 follow-up PRs (#4177 fallback flag, #4181 SSE rate cap, #4185 token cap, #4190 cluster budget reset, #4191 long-inserted threshold, #4193 Flash Lite empty-output, #4210 BoN error fallthrough + ffprobe) all caused by missed per-rung invariants — plan-review caught the headline diagnosis but missed each candidate's resource accounting. See `feedback_bon_cascade_per_cand_invariants_plan_check.md`.' 26. CUSTOMER ARTIFACT DELIVERY INVARIANT (HARD RULE per Rajiv CTO directive 2026-05-26 10:57 IST thread `1779763828.526989`): For auto-process, proofread, format, BoN, validator, and export paths, the product default is to deliver the best usable content-safe output and surface warnings. Do not hard-reject, roll back, block draft readiness, or discard a non-empty usable candidate because of validator uncertainty, score thresholds, formatting drift, co-occurrence ratios, or quality concerns. Those must become structured warnings and `ready_with_warnings`. Hard reject is allowed only when delivery is impossible or unsafe: no candidate exists, persistence/export is impossible, auth/security/privacy fails, or the artifact would be materially corrupt in a way the user cannot reasonably inspect/recover from. Any threshold controlling validator severity, BoN rescue, warning vs hard block, or candidate selection must be configurable through the existing Modal BoN/validator threshold config. New magic-number thresholds are a review blocker. **Plan review gate shape**: REJECT plans where severity dies at the cross-boundary contract: `CandidateValidationResult → LadderResult → validator_result → deriveDraftReadiness`. Any plan that does not preserve severity_class + delivery_decision across all four boundaries fails this gate. Plans introducing new validator thresholds, BoN rescue thresholds, or candidate-selection thresholds MUST surface them as config-driven (existing Modal threshold config), NOT magic numbers. Plans that propose hard-reject / fail-open-to-fallback / rollback semantics on quality concerns MUST be REJECTED or REVISED with concrete redirect to warn-not-reject + `ready_with_warnings` delivery. (LLM PROXY / STT FIXTURE DETERMINISM PLAN GATE — HARD RULE, PR #5140 retro 2026-05-30): If the plan touches `modal/shared/llm_proxy_server.py`, WhisperX/AssemblyAI/TSVAD payloads, ASR/STT fixture hashing, hotwords, proxy-only metadata, fixture capture, or strict replay, REVISE/REJECT unless it includes executable ACs for: same canonical decoded audio content -> same STT fixture key across dynamic IDs, filenames, URLs, and container encodes; different audio -> different key; hash input excludes raw container bytes, URLs, filenames, IDs, timestamps, and hotwords; proxy-only metadata such as `source_audio_digest` is normalized for proxy hashing and stripped upstream; every sync/callback/fallback entrypoint receives the same metadata path or is proven unaffected; tests fail closed on repo fixtures and exercise production proxy path or TestClient+upstream spy; implementation streams/chunks decoded audio hashing; readiness needs latest-head capture plus latest-head strict replay, not capture alone. 27. CUSTOMER-DATA REDACTION IN COMMITTED ARTIFACTS (HARD RULE per Codex meta-analysis 2026-06-22 PR #5703 trigger: customer-derived transcript examples): If the plan proposes committing prompt examples, fixtures, investigation docs, plan docs, or test data DERIVED from a customer file (a feedback report, deposition, transcript, support upload, or in-app diagnostics artifact such as a feedbackId/transcriptId), the plan MUST require that real customer-derived content be ANONYMIZED before it lands in any versioned file or CI artifact. For a court-reporting product, real-looking deposition names, testimony fragments, and repeated customer artifact/thread/template identifiers from a customer file must not be preserved in the repo or CI logs. The plan MUST use synthetic placeholders for any example text that demonstrates the behavior (the production prompt/code should already use synthetic placeholders — the docs/tests must match). REVISE plans that extract examples from a named customer file (e.g. "examples derived from kx7d3kz5") and commit them verbatim into docs/plans/*, docs/investigations/*, tests/fixtures/*, or prompt strings without an anonymization step. Verdict text on REVISE: 'Plan commits customer-derived transcript/deposition content into versioned docs/tests. Anonymize all real names, testimony fragments, and customer artifact/thread identifiers to synthetic placeholders before merge — customer-derived deposition content must not be preserved in the repo or CI artifacts.' Reference: PR #5698 plan + investigation docs committed real names/transcript fragments extracted from feedback file kx7d3kz5; the production prompt used synthetic placeholders but the docs did not. 28. RUNTIME CONTROL POINT CONSISTENCY (Codex meta-analysis 2026-06-27 PR #5844 trigger: plan-review P1#1): The CLAUDE.md directive-translation gate already requires a plan to name its runtime control point (the gate / state transition / handler / emit-site / persistence boundary the change must touch). This check adds the CONSISTENCY half: the plan MUST state that control point IDENTICALLY across its Intent/Capability section, its Implementation section, its Out-of-Scope section, and its AC ledger. A plan that says the change executes at one point in one section and a contradictory point in another (e.g. "classify BEFORE project persistence / before createExternalProject" in the intent + AC sections but logs AFTER createExternalProject in the implementation steps, and asserts the opposite again in Out-of-Scope) has not chosen its contract — the slot will implement one reading, tests will assert another, and reviewers cannot tell which is the spec. REVISE any plan whose stated runtime control point (pre-create vs post-create, before-guard vs after-guard, sync vs scheduled, correlationId vs persisted projectId availability) differs between sections. Verdict text on REVISE: 'Plan states its runtime control point inconsistently across sections (intent says X, implementation says not-X, out-of-scope says X again). Pick ONE contract and state it identically in the Intent, Implementation, Out-of-Scope, and AC sections — including what correlation/identity data is available at that point (e.g. correlationId pre-create vs projectId post-create).' Reference: PR #5844 plan (issue #5839 ingest observability) contradicted itself on pre-create vs post-create logging across three sections; the companion plan-review flagged it P1 and the impl had to pick the pre-create boundary. 29. DETERMINISTIC MULTI-CLASS CLASSIFIER / OBSERVABILITY TAXONOMY COMPLETENESS (Codex meta-analysis 2026-06-27 PR #5844 trigger: plan-review P1#2 + GitHub Codex BOT P2): When the plan designs or modifies a DETERMINISTIC classifier / observability taxonomy / debug-event router that maps an input into ONE OF N mutually-exclusive classes (the 6 ingest debug classes, a degraded-reason taxonomy, an error-class router, a status enum), the plan MUST prove TOTALITY before approval. (1) Enumerate every declared class, then enumerate every reachable (input-shape × processing-terminal-outcome) combination and show each lands in exactly one declared class — explicitly including the empty/`null`/`""`/non-string field-value cases AND the "processed successfully but produced no useful output" terminal outcomes (jobsheet extracted but no roster). (2) PRESENCE-FLAG vs USABLE-VALUE parity: if a discriminator/`had*` flag is computed from RAW field presence (`'x' in body`, key-count > 0) but a downstream branch only ACTS on the field when USABLE (truthy non-empty string, non-empty extraction), the plan MUST either compute the flag with the SAME predicate the processing branch uses OR declare a distinct invalid-input class — otherwise an ignored/invalid value is reported as supplied and falls through to an ADJACENT default class, misattributing the telemetry the taxonomy exists to disambiguate. REVISE plans that declare an N-class taxonomy without a class-by-input totality table and without proving the presence flag matches the processing predicate. Verdict text on REVISE: 'Plan designs an N-class classifier/observability taxonomy but does not prove every reachable (input-shape × processing-outcome) maps to exactly one class. Add a totality table covering empty/null/non-string field values and processed-but-empty outcomes, and confirm each had-field flag uses the same predicate as the processing branch (or add a distinct invalid-input class) so ignored/invalid inputs are not reported as supplied and silently misclassified into an adjacent default.' Reference: PR #5844 (issue #5839) — `jobSheetUrl: ""`/`null`/non-string with no metadata was flagged jobsheet-provided yet skipped processing and fell through to `external_project_metadata_no_roster` instead of `external_project_no_metadata_inputs` (BOT P2, deferred to #5852); a jobsheet that extracted but yielded no roster had no dedicated terminal class (companion plan-review P1#2).  30. MODAL/AUDIO + MODAL/SHARED COPY PARITY (Codex meta-analysis 2026-07-02 PR #5997 trigger: plan-review R1 REVISE, sibling-copy-parity class): When the plan modifies any file in `modal/audio/` that has a documented mirror in `modal/shared/` — especially `modal/audio/section_detection.py` ↔ `modal/shared/section_detection.py` — the plan MUST include BOTH copies in its changeset and add a parity test that exercises both `_audio_sd` and `_shared_sd` variants. Updating only one copy creates runtime parity drift (the production Modal `/format` runtime bundles `modal/shared/` directly; a slot running `modal/audio/processor.py` uses the `modal/audio/` copy). REVISE if the plan lists only one copy. Verdict text: "Plan changes `modal/audio/section_detection.py` without including `modal/shared/section_detection.py` (or vice versa). Both copies must be updated together. Update the changeset to include both files and add a parity unit test proving the behavior is identical across both copies." Pattern (from PR #5936/#5997 plan R1): plan listed only `modal/audio/section_detection.py` regex change; `modal/shared/section_detection.py` mirror was absent from the changeset and parity test; R1 REVISE triggered; fixed by R4. 31. BRANCH DIFF SCOPE CONTAMINATION (Codex meta-analysis 2026-07-07 PR #6175 trigger: 7 plan-review rounds caused partly by contaminated branch diff): When the plan declares a scoped change (e.g., "UI-only", "frontend-only", "no modal/ or convex/ changes", "components/ only", "no pipeline changes"), verify the actual branch diff file list matches the declared scope. If the diff includes files that directly contradict the declared scope — e.g., `convex/`, `modal/`, `benchmarks/`, E2E seed, or plan docs from OTHER issues on a "UI-only" issue — REJECT with the exact conflicting file list: "Branch diff contains out-of-scope files contradicting the plan's declared scope of [X]. Files outside scope: [Y]. The slot must create a fresh branch from main with only in-scope changes, or expand the plan scope to cover and justify the extra files. Out-of-scope files often indicate the branch accumulated changes from prior sessions or other issues — check `git log origin/main..HEAD --oneline` and `git diff origin/main...HEAD --name-only`." Do NOT flag files that are legitimately in scope (plan doc for this issue, tests for the changed component). Only flag files that contradict the declared change type (pipeline files on a UI-only plan, schema files on a frontend-only plan). Reference: PR #6175 (issue #6162 UI-only precedence warnings) — initial branch `fix/6162-indent-precedence-ui` included `convex/`, `modal/`, benchmarks, PRM scripts, and E2E seed from accumulated prior-session work; Codex R1 correctly rejected on scope; slot had to create a clean branch, causing 7 total plan-review rounds. VERDICT: APPROVE, REVISE (with specific feedback), or REJECT (with reason)

LONG-FILE EDITOR RESPONSIVENESS CONTRACT (HARD RULE per Rajiv CTO directive 2026-07-20): Treat `tests/e2e/specs/core/smoke-large-file-perf.spec.ts` with `SEED_PROJECT_LONG` as a release-critical baseline against user-visible main-thread lag. Trigger this gate by runtime reachability/file/symbol, not issue wording, when a plan touches `components/editor/extensions/CSSPagination/**`, `hooks/use-audio-highlighting.ts`, `hooks/use-transcript-editor.ts`, `lib/editor/stores/WordTimingStore.ts`, `lib/editor/store-reconciliation.ts`, `lib/editor/position-index-lifecycle.ts`, `lib/editor/audio-time-transport.ts`, or ProseMirror/bulk-edit flows feeding them. Require a `Long-file responsiveness impact` ledger with `Hot path | Triggering interaction | Runtime control point | Frequency/complexity | Scheduling/offload boundary | Focused proof | Long-file E2E assertion`; cover representative long-file load, editing, scrolling, playback/seek/highlighting, and concurrent editing+playback. Missing telemetry, undersized seed, unavailable required audio, or skipped/conditional scenarios must fail closed. Threshold relaxation requires same-seed/same-runner baseline evidence plus explicit CTO approval; disabled instrumentation must preserve prior hot-path cost. Manual/local/qa proof is supplemental. Pure prerequisites may delegate only to a named runtime issue retaining the guard. Missing, vague, or partially coupled coverage = REVISE/REJECT.

## Architecture-to-plan control-point consistency (mandatory for every review)

### Architecture contract availability

If an architecture-review contract is present, compare the plan against it.

If it is absent, stale, or does not identify the directive source, production
case, entrypoint, runtime trace, control point, reachability evidence, and split
decision, independently reconstruct those facts from the issue, authoritative
comments, and current code. Do not assume architecture approval from a label or
issue statement.

Return these exact fields:

ARCH_CONTRACT_STATUS: MATCHES | STALE | MISSING
DIRECTIVE_FIDELITY: MATCHES | NARROWS | DRIFTS | UNKNOWN
RUNTIME_CONTROL_POINT: <boundary>
REACHABILITY: PROVEN | UNPROVEN | DISPROVEN
SCOPE_DECISION: SINGLE | SPLIT_REQUIRED
EDITOR_PERF_GUARD: NOT_TRIGGERED | PASS | REVISE | REJECT
HOT_PATHS: <files and symbols, or none>
AFFECTED_SCENARIOS: <load/edit/scroll/playback/seek/highlight/concurrent windows>
MAIN_THREAD_CONTROL_POINTS: <work and scheduling/offload boundaries>
BASELINE_EVIDENCE: <same-seed/same-runner receipt, or missing>
THRESHOLD_CHANGE: none | approved | unapproved
FAIL_CLOSED_PROOF: <telemetry, seed, audio, and scenario availability proof>
RESCOPE_STATUS: NONE | PROPOSED | RECORDED
RESCOPE_TYPE: none | split | defer | narrow | control_point_change | capability_change
ORIGINAL_REQUIREMENT: <original issue/directive requirement, or none>
NEW_SCOPE: <replacement scope, or none>
RESCOPE_REASON: <why the scope changed, or none>
RESCOPE_EVIDENCE: <runtime/test/artifact/dependency evidence, or missing>
RESCOPE_OWNER: <named owner, or missing>
FOLLOW_UP_ISSUE: <linked issue, none, or missing>
RESCOPE_APPROVER: <named approver and evidence, or missing>
ISSUE_CONTRACT_UPDATED: yes | no | not_applicable
PR_SUMMARY_UPDATED: yes | no | not_applicable

STRUCTURED RESCOPE EVIDENCE CONTRACT (Rajiv CTO directive 2026-07-20): The GitHub issue is the authoritative scope contract. For any split, deferral, narrowing, runtime-control-point change, or intended-capability change, require the issue body or authoritative issue comment to contain every rescope field above. Deferred/split work needs a named owner and linked follow-up issue. A Rajiv-directed capability change requires explicit Rajiv approval. If a PR exists, require its description to summarize the rescope and link to the issue record. A plan-only or PR-only rescope is invalid. If the issue still promises the old work, REVISE/REJECT and set `TERMINAL_DISPOSITION: ISSUE_REWRITE_REQUIRED`.

REJECT when REACHABILITY is DISPROVEN. REVISE when REACHABILITY is UNPROVEN.
Set TERMINAL_DISPOSITION: ARCH_REVIEW_STALE when the plan changes the approved
capability, entrypoint, service boundary, state transition, affected population,
or split decision.

Verify that the implementation plan preserves the architecture review's
production case, entrypoint, runtime trace, and approved control point.

The plan must show, for every proposed code change:

Production case → entrypoint → runtime path → control point → code change →
failing proof → passing proof.

REVISE or REJECT when:
- the plan targets a different control point from the architecture review;
- the plan names a function or callback without proving the affected case calls
  it;
- the test exercises copied or simulated logic rather than the production path;
- the plan omits an adjacent path that could be mistaken for the affected path;
- existing-state repair and forward prevention use different control points but
  remain combined;
- the plan materially changes scope or runtime architecture without requesting
  a fresh architecture review.

If the proposed control point is unreachable, stop the review at this finding.
Do not continue producing downstream implementation or test-shape revisions for
an invalid architectural premise.

### Review convergence

Assign every blocking finding a stable class and ID: DIRECTIVE, CONTROL_POINT,
REACHABILITY, SCOPE, IMPLEMENTATION, or PROOF.

On re-review:
- first state whether each prior blocking finding is resolved;
- introduce a new blocker only when it was caused by the revision or could not
  previously be inspected;
- do not restate the same concern under a new description.

If DIRECTIVE, CONTROL_POINT, REACHABILITY, or SCOPE remains blocking for two
rounds, stop ordinary review, set VERDICT: REJECT, and return exactly one
terminal disposition:

TERMINAL_DISPOSITION: ARCH_REVIEW_STALE | SPLIT_REQUIRED |
ISSUE_REWRITE_REQUIRED | PM_ADJUDICATION_REQUIRED

Do not request a third ordinary revision for the same architectural class.

<!-- Codex meta-analysis 2026-07-10 PR #6233 trigger: plan-review-call-site-audit-completeness -->
32. CONVEX RETURN CONTRACT CALL-SITE AUDIT: When a plan adds or changes a field in a Convex query or mutation return shape (e.g., adding `keyValid` to `checkRateLimit`, adding a new field to a query result, renaming a return property), the plan MUST include an explicit "Affected Tests" section that: (a) greps ALL test files (not just the primary test file) for the function name and its mock pattern (e.g., `checkRateLimit`, `retryAfterMs`, `allowed: false`); (b) lists EVERY matched file with a classification of `unaffected` (existing mock uses only the always-present fields) or `update required` (mock returns an object that now has a new discriminator field the test path exercises); (c) total count of files checked and updated. Missing this section or partial enumeration that omits call sites found by the same grep produces R2/R3 REVISE churn. If the plan lists the audit but a grep finds additional files not mentioned, mark those files explicitly as `unaffected — mock returns { allowed: true }` or equivalent. The R3 trigger for PR #6233 was `__tests__/app/api/templates/route.test.ts` omitted from an otherwise "complete" audit. Incomplete call-site audit = REVISE.
HARD PRODUCT RULE — SEMANTIC TRANSCRIPT OWNERSHIP:

- The stage where a defect becomes visible is not automatically its owner.
- In the AI transcript pipeline, semantic speaker tracking is owned exclusively
  by the speaker-correction prompt. This includes speaker attribution/identity,
  same-speaker continuation, turn merge/split, Q/A turn ownership, bad-turn
  repair, speaker label/name mapping, and unidentified-speaker resolution.
- AI formatting may render already-correct speaker structure, but it must never
  infer, repair, coalesce, split, or rewrite speaker tracking. Validators may
  detect, warn, or reject; they must not repair semantic speaker state.
- GT/user-invoked layout regeneration is a separate deterministic conversion
  boundary. It may parse explicit speaker labels already present in the GT
  source, preserve those authoritative identities, and group utterances for the
  selected layout. TurnId grouping and explicit-label parser defects there are
  valid algorithm bugs; the converter must not invent an absent identity.
- Contextual filler removal is owned exclusively by the proofreading prompt.
  It must never be implemented through regex, DSL, deterministic cleanup,
  formatter/parser transforms, editor rebuilds, or another post-processor.
- Both prompt changes require anonymized, artifact-derived examples from an LCS
  word/paragraph diff between the human-edited latest proceedings and the
  auto-process artifact for the same file, including KEEP/negative examples.

If an issue or plan proposes an AI-pipeline post-processor for either semantic
capability, REJECT it as MISFILED, set `TERMINAL_DISPOSITION:
ISSUE_REWRITE_REQUIRED`, and require the contract and plan to target the owning
prompt. Do not apply this rejection to deterministic GT layout conversion that
preserves explicit source speaker identity. Tests must drive the live owning
path and assert the resulting structure or contextual filler outcome, not merely
a helper, regex, AI formatter, or frozen prompt string.

Return:
SEMANTIC_OWNER: SPEAKER_CORRECTION_PROMPT | PROOFREADING_PROMPT | OTHER
OWNERSHIP_VERDICT: MATCHES | MISFILED

<!-- REVIEW_CONVERGENCE_POLICY_V1 -->
CONVERGENCE PREFLIGHT: First verify the current `origin/main` SHA, the production
entrypoint, owning runtime control point, downstream consumers, and a
RED-on-baseline / GREEN-on-change / RED-on-revert proof. Apply only review gates
whose trigger is reachable from that entrypoint. Unresolved ownership or
reachability is one explicit blocker, not an invitation for implementation-time
architecture discovery.

For every blocking finding emit a complete stable blocker record using the
marker contract. On re-review reuse the prior ID and fingerprint, and close or
preserve each prior blocker before introducing a revision-caused or
newly-inspectable blocker.

<!-- BEHAVIORAL_AC_PROOF_CONTRACT_V1 -->
BEHAVIORAL AC PROOF GATE: For every behavior-changing AC, require before
implementation handoff the production case/entrypoint, owning runtime control
point, exact runnable verification command, deterministic fixture or
production-shaped evidence, expected observable, and negative/regression case.
`TBD`, `planned`, helper-only proof when the production path is runnable,
generic "tests pass", or deferring proof discovery to paid CI is REVISE.
Phase 4 may attach receipts; it must not invent the proof strategy after code
exists.

<!-- SPLIT_SEAM_COHESIVE_TRANSACTION_V1 -->
SPLIT-SEAM / COHESIVE RUNTIME TRANSACTION GATE (Codex meta-analysis 2026-08-10):
For any plan spanning multiple surfaces (capture workflow, deployment/rollout,
log collection/observability, product runtime, schema/migration, or test
harness), require the plan to state either (a) one cohesive runtime transaction
the change satisfies, with the reachable control point proven, or (b) an
explicit split seam with a named owner and linked follow-up issue per
independently shippable piece. Do not auto-split from file/diff/surface counts.
A rescope signal is not itself proof the original plan lacked a reusable
control; re-verify the named runtime transaction and split-seam justification.
REVISE any multi-surface plan that omits the justification or splits without
owners/follow-ups. Do not loop ordinary revision rounds on this class more than
once; after a second same-class block set `TERMINAL_DISPOSITION:
SPLIT_REQUIRED` (or `ISSUE_REWRITE_REQUIRED` when the issue itself spans
incompatible surfaces).


32. VALIDATOR HARD-REJECT PLAN GATE (issue #7944 lineage — a validator string is not a corruption witness): When the plan touches validators, severity policy, BoN candidate selection/fallback, exception mapping, readiness derivation, or terminal fail-open behavior, REJECT plans that hard-reject (reject usable candidate, retry solely for a preference, force original fallback, fail readiness/pipeline completion) over a heading/template/blank-line/casing preference. Reference negative example: cover formatting failed with `cover_structure_contract:missing immutable anchor: jurisdiction`; executed discriminators on main `6793164e` prove blank-line-only and `DEPOSITION`→`Deposition`-only changes delete no content, yet `processor.py` promoted both to catastrophic/hard failure. The plan MUST instead specify: deterministic existing DSL normalization of COVER HEADING to `DEPOSITION` (heading-only, idempotent), structured warning logs for missing metadata and benign wording/casing/layout differences, usable output selectable and deliverable (`ready_with_warnings`), no new popup/status schema — plus an explicit no-repromotion contract across CandidateValidationResult → LadderResult → validator_result → deriveDraftReadiness (a soft result re-promoted downstream is a must-fix defect). Approve warning-only handling and DSL correction; retain witnessed-corruption controls (word-level LCS + deleted span); do not weaken auth, parseability, transport, save CAS/version, or loss/duplication safeguards. Verdict text on REJECT: 'Plan hard-rejects (or re-promotes to rejection) over a validator literal without a concrete corruption witness. Re-shape as DSL normalization + structured warnings with deliverable output, or supply word-level LCS evidence of deleted content.'
